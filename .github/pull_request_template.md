## What changed

<!-- Briefly describe the change and why. -->

## Checklist

- [ ] `make test` passes (Go build/vet/test, plus `npm run lint && npm run typecheck && npm run build`)
- [ ] No personal files are added (`*.pdf`, `data/*.yaml`, `.env`, `.kilo/`, `.cursor/`, `.specify/`)
- [ ] No fabricated candidate facts; cover letters stay grounded in `project_bank.yaml`
- [ ] If I added a job source, it uses a public documented API and has a test with a captured fixture
- [ ] Docs updated (`README.md`, `CHANGELOG.md`) if behaviour or configuration changed

## Notes for reviewers

<!-- Anything you are unsure about, or alternatives you rejected. -->